import styled from '@emotion/styled'

const Code = styled.div`
  background-color: ${(props) => props.bg || '#202023'};
  font-family: 'Cascadia Code', sans-serif;
  color: #fff;
  padding: 1rem;
  border-radius: 0.5rem;
`

export default Code
